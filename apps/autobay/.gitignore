# Dependencies
node_modules/
.pnpm-store/

# Next.js
.next/
out/

# Environment (never commit secrets)
.env.local
.env.*.local

# Database (local SQLite, not for source control)
prisma/*.db
prisma/*.db-journal
prisma/*.db-wal
prisma/*.db-shm

# Uploads (user files, not for source control)
public/uploads/*/

# OS
.DS_Store
Thumbs.db

# Debug
npm-debug.log*
pnpm-debug.log*
