# Session State

## Last Updated
2026-08-03 (**Forge V1 tagged.** Tag `forge-v1.0.0` (annotated), target commit `9181d56`. Release checkpoint commit `24bf3d4`, production application commit `9cd737b` (all commits between them are documentation-only). Readiness verdict: **READY WITH NON-BLOCKING FOLLOW-UPS** (`docs/releases/forge-v1-readiness-audit.md`). GitHub Release published at the same tag (not draft, not prerelease). Release record: `docs/releases/forge-v1-release.md`. Production deployment `dpl_DzZoCf6zBRqxrNe4QK1zbfDRwT5k` READY, aliased to `app.ppmnky.com`, serving the tagged commit. PPM confirmed blank (0 customers/jobs/quotes) and Demo confirmed healthy (2 customers, 2 jobs, 3 quotes) throughout, unchanged. Known non-blocking follow-ups: F2 (triage-state desync), F4 (duplicate UI path), F6 (`customer_archetype_defaults` RLS), F7 (e2e-bot accounts on PPM), the `service_requests` broad-authenticated-write pattern (flagged for a future dedicated security batch, not Batch B/D), Resend not configured, numbering not per-organization. **Next approved work has not yet been selected** — awaiting Kevin's choice among `service_requests` hardening, the Base44 compatibility spike, or another non-blocking batch. Base44 remains unstarted.)

### Prior entry (2026-08-03, Forge/Foundry naming rename)
**Forge/Foundry naming rename deployed to production and verified (Phase F2 complete).** PR #89 merged (squash commit `8da54d7`, main HEAD `8da54d7`), Vercel deployment `dpl_CRw8Pbyb7A8pGjEgsgYKZUeE6nAz` READY, aliased to `app.ppmnky.com`, confirmed serving the merged commit before the production migration ran. The Demo organization's **display name** changed live in production from "Premier CRM Demonstration" to **"Forge Demonstration"** via `20260803060000_forge_demo_org_display_name.sql` (`npx supabase db push --linked`); id (`a0c9b59d-...`), slug (`premier-crm-demonstration`), timezone, memberships (3), customers (2), properties (3), requests (4), and invoices (5) all confirmed unchanged by direct query. PPM organization confirmed unchanged (`Premier Property Maintenance LLC`, still blank — 0 customers, 0 requests) and untouched by the migration (`updated_at` predates it). Live production verification via real browser: root/login title = "Forge", staff sign-in heading = "Sign in to Forge", PWA manifest name/short_name = "Forge", customer portal doorway/login stay tenant-branded ("Premier Property Maintenance — Customer Portal" title, "PREMIER PROPERTY MAINTENANCE" heading, no Forge in headings), public PPM marketing site (`ppmnky.com`) confirmed completely unchanged. Post-deploy `pnpm test` 159/159, `pnpm typecheck` clean on the merged `main`. Repository-wide naming search re-run post-deploy — no unresolved active product-facing "Premier CRM" reference found; all remaining matches are historical narration, stable technical identifiers, or deferred non-customer-facing test-suite comments.

## One coherent story so far

1. **Production stabilization (Milestone A) — ACHIEVED.** PR #78 and PR #79 merged and deployed.
2. **Production cleanup and Jobber-import purge — EXECUTED AND VERIFIED.** `docs/production/cleanup/2026-08-01-production-cleanup.md`. Production was a true blank slate going into everything below.
3. **Storage/upload architecture — verified via spikes, then implemented for real.**
4. **Site-visit workflow — implemented and verified on `premier-crm-e2e`.** `docs/implementation/request-site-visit-estimate-workflow.md`.
5. **Final merge-readiness audit on PR #80 — found and fixed 4 cross-org authorization defects** (TypeScript layer; SQL/RPC layer audited clean).
6. **Marketing-site PR #5 and CRM PR #80 merged and deployed to production.** Marketing-site merge commit `8807fa9`. CRM merge commit `15078a3`.
7. **A fifth production authorization defect was found during controlled production validation** — `sendQuoteAction()`/`resendQuoteEmailAction()` checked the wrong capability. Fixed (hotfix PR #81, commit `9a376b3`, merge `7334c3a`), covered by 9 regression tests. `docs/production/deployments/2026-08-02-site-visit-workflow-deployment.md` §2.
8. **Full production smoke-test chain completed and fully cleaned up.** Production confirmed back to a verified blank slate afterward.
9. **Premier CRM Demonstration organization created in production.** Multi-tenant blocker in `getActiveOrgContext()` fixed first (PR #82, merge `2d51546`) via a `user_profiles.active_org_id` preference + guarded `switch_active_org()` RPC, deterministic oldest-membership fallback, zero call-site changes.
10. **Demo org bootstrapped via a repeatable, idempotent, `service_role`-only RPC** (`bootstrap_demonstration_organization()`). Org ID `a0c9b59d-77d9-48ad-9760-8555c9ed8fe5`, slug `premier-crm-demonstration`, timezone `America/New_York`. 12 system-default automation rules auto-seeded by the pre-existing `on_organization_created` trigger.
11. **Kevin added to Demo as owner** (`org_members.id = 05521ad6-...`). His original PPM membership is unchanged; `getActiveOrgContext()` still defaults him to PPM with no explicit preference set.
12. **Multi-tenant isolation verified** at both the mechanism level (Phase 3) and record-by-record throughout population (Phase 4) — every write confirmed `org_id`-scoped to Demo, PPM counts checked before/after every stage.
13. **Brandon's real account status was inspected, not acted on.** Real unconfirmed auth user, no PPM membership, one pending PPM invite (expires 2026-08-15). **Still not added to Demo, not re-invited, not modified.**
14. **The Premier CRM Demonstration organization was fully populated with a permanent dataset** covering all three triage paths, in production, verified. Full record: `docs/implementation/premier-crm-demonstration-organization.md` §11 and `docs/implementation/premier-crm-demo-dataset-manifest.md`.
    - **Stage 1**: Dana Whitfield (residential, 1 property) and Bramwell Retail Group (commercial, 2 properties) created; Dana's permanent customer-portal account created.
    - **Stage 2 (Scenario A — remote estimate)**: request → triage → estimate → pricing approval → quote sent, correctly totaling $230.00.
    - **Stage 3 (Scenario B — site-visit lifecycle, the polished showcase)**: request → triage → schedule → **reschedule** (history preserved) → start → 2-pass inspection (2 real photos) → complete → estimate (idempotency proven) → quote ($455.00) → accepted → job → scheduled → deposit ($150, paid) → change order (customer-approved, incorporated once) → working invoice ($520.00) → final invoice ($520.00, paid).
    - **Stage 4 (Scenario C — direct work order)**: request → triage `direct_work_order` (`standing_agreement`, NTE $350) → **authorization boundary proved live** (temporary Demo-only employee/subcontractor test accounts both correctly denied `canCreateDirectWorkOrder` at the RPC, zero mutation, then deleted) → exactly 1 job, no estimate/quote → scheduled → working invoice ($240, under NTE) → final invoice (paid).
    - **Stage 5**: temporary employee/subcontractor identities fully deleted. The temporary "driver" identity used to perform ~30 population actions could **not** be deleted (FK-referenced as the audit-trail actor on 32 permanent records — deleting it would falsify who performed the work) — retained but rendered fully inert (zero org memberships anywhere, banned ~100 years, synthetic non-deliverable email).
15. **Three real, pre-existing production defects were found and fixed during population** (all found, contained, hotfixed, tested, deployed before continuing — same discipline as the earlier `canSendQuote` incident):
    - **PR #83**: no application path ever created a `kind='deposit'` invoice (`job_deposits.deposit_invoice_id` existed but nothing set it). Added `createDepositInvoice()`, gated by the existing `canManageDeposits` capability.
    - **PR #84**: `create_quote_from_estimate()` never recalculated quote totals — every RPC-created quote was left at $0.00. Fixed with a SQL trigger (`recalc_quote_totals()`) on `quote_line_items`.
    - **PR #85**: `generateFinalInvoiceFromWorking()` had the identical bug on the invoice side. Fixed the same way (`recalc_invoice_totals()` trigger on `invoice_line_items`).
    - All three merged to `main`, migrations applied to both `premier-crm-e2e` and `premier-crm-prod`, deployed, covered by new permanent regression tests.
16. **Brandon's Demo onboarding was started, then explicitly deferred by Kevin** before first-login verification. A real structural conflict was found first (the real accept-invite flow refuses to activate anything when a user has more than one pending invite — Brandon already had a pending PPM one, which Kevin's instructions forbid touching) and Kevin approved a one-time exception: Demo `org_members` row created directly (`ebdd5826-...`, employee), email confirmed via the Admin API, a Supabase-native password-reset email triggered. Kevin then changed plans before Brandon's first login — Brandon's account is preserved exactly as left (Demo membership intact, PPM invite untouched), but no login/capability verification was performed. Full status: `docs/implementation/brandon-demo-onboarding-and-observation.md`.
17. **Kevin created his own dedicated Demo employee test account** — his real, pre-existing `sommerskevin3@gmail.com` account (already a legitimate PPM employee, one of the 4 pre-existing PPM members) was added to Demo as `employee` (`org_members.id = ec13d3e3-...`). A repeatable "Kevin UI Observation Scenario" training record (`SR-000012`, site visit `2422de29-...`, left `awaiting_scheduling`) was prepared for him to work through manually via the real UI. Observation framework: `docs/implementation/kevin-demo-ui-observation.md`.
18. **Kevin's first UI-observation checkpoint fixed** (PR #86, commit `96a40e6`): raw "Approve pricing" button/error shown to employees replaced with a real employee-to-owner pricing-review handoff (`pricing_review_status`, submit-for-review, return-for-changes-with-note, line-item locking while pending); stale "Schedule site visit" control removed from site-visit-generated estimates.
19. **Pre-Base44 Workflow Refinement phase complete** (PR #87, commit `cca3ba7`, deployed and verified): role-aware "Needs your attention" Today action queue (pricing-review/create-quote/send-quote tasks, capability-gated, org-scoped, no separate dismissal state — see `packages/db/queries/today-actions.ts` and `docs/ux/base44-handoff.md` §5); mobile bottom-nav badge-crowding fix (corner-positioned badge, safe-area padding); inspection-form measurement/quantity/material list fields given visible per-column labels (`COLUMN_META` in `inspection-form.tsx`) fixing the ambiguous raw-key placeholder issue from Kevin's observation. New `today-action-queue-bot.spec.ts` (7 tests). Design-only deliverables written, not implemented: `docs/ux/hazards-section-proposal.md`, `docs/ux/request-list-density-recommendation.md`, `docs/ux/base44-handoff.md`, `docs/ux/base44-compatibility-spike-plan.md`.
20. **Forge/Foundry naming audit (Phase F1, read-only)** — PR #89, `docs/architecture/forge-foundry-naming-audit.md`. Full inventory/classification of every "Premier CRM" reference across the app, tests, and docs; confirmed customer-facing email templates, the portal doorway, and the org switcher already correctly separate product identity from tenant identity; flagged the persisted Demo-org display name as the one item needing Kevin's explicit decision. No code changed in this phase.
21. **Forge/Foundry naming rename deployed to production (Phase F2 complete)** — Kevin approved the audit with 10 explicit decisions; PR #89 merged (squash commit `8da54d7`), deployed (Vercel `dpl_CRw8Pbyb7A8pGjEgsgYKZUeE6nAz`, READY, `app.ppmnky.com`), and verified. Implemented: `packages/shared/brand.ts` (`PRODUCT_NAME = 'Forge'`, `ECOSYSTEM_NAME = 'Foundry'`); root layout title template (`Forge — %s`) + per-route titles for Today/Customers/Properties/Requests/Site Visits/Estimates/Quotes/Jobs/Invoices/Settings; PWA manifest name; staff sign-in heading; the one internal staff-notification email template; portal copy updated only where it described the software (portal doorway/login headings stay tenant-branded per Kevin's decision 8, portal doorway given an absolute "Premier Property Maintenance — Customer Portal" title); README/`.env.example`/`CLAUDE_CONTEXT.md`/`01-PRD.md`/`RESUME_PROMPT.md` prose; new `docs/architecture/forge-foundry-brand-boundaries.md`. Demo organization's **display name** changed live in production to "Forge Demonstration" via `20260803060000_forge_demo_org_display_name.sql` (`npx supabase db push --linked`) — id/slug/timezone/memberships/customers/properties/requests/invoices all confirmed unchanged by direct query. PPM organization confirmed unchanged and untouched by the migration. Live production verification (real browser, both authenticated-shell fallback and public portal pages): root/login title "Forge", staff sign-in "Sign in to Forge", PWA manifest "Forge", customer portal stays tenant-branded, public PPM marketing site (`ppmnky.com`) fully unchanged.

## Clean Checkpoint (current, post Phase F2 deployment)
- `main` HEAD: `8da54d7` (PR #89 squash-merge).
- Production deployment: `dpl_CRw8Pbyb7A8pGjEgsgYKZUeE6nAz`, state `READY`, aliased to `app.ppmnky.com`, confirmed serving commit `8da54d7` before the production migration ran.
- Production migration `20260803060000_forge_demo_org_display_name.sql` applied via `npx supabase db push --linked`. Demo org: `Forge Demonstration` (was `Premier CRM Demonstration`), id `a0c9b59d-...` and slug `premier-crm-demonstration` unchanged. PPM org: `Premier Property Maintenance LLC`, unchanged, still blank (0 customers, 0 requests).
- `pnpm test`: 159/159 pass. `pnpm typecheck`: clean across all packages. Production build confirmed via successful Vercel deployment of this exact commit.
- Repository-wide naming search re-run post-deploy: no unresolved active product-facing "Premier CRM" reference; remaining matches are historical narration, stable technical identifiers, or deferred non-customer-facing test-suite comments.

## Current Branches
- CRM: `main` at `8da54d7` (includes PR #80–#89). No feature branches pending.
- Marketing site (`premier-property-maintenance`): `main` at `8807fa9` (unchanged this phase — not touched by the Forge rename, confirmed no public-facing product-name references exist there, and confirmed live-unchanged via direct browser check of `ppmnky.com`).

## Current Goal
Both real organizations (Premier Property Maintenance LLC and Forge Demonstration, née Premier CRM Demonstration) exist in production; Demo has a complete, permanent, verified demonstration dataset. The Pre-Base44 Workflow Refinement phase and the Forge/Foundry naming rename (Phase F2) are both implemented, deployed, and verified in production. Next, in order (each requiring Kevin's separate explicit authorization): (1) Forge V1 readiness audit; (2) Base44 compatibility spike (plan prepared in `docs/ux/base44-compatibility-spike-plan.md`, not run); (3) Forge V1 baseline tag. Not yet authorized: Brandon's further onboarding, any V1 tag, Base44 implementation work, Resend configuration, repository/infrastructure renames.

## Business policy decisions locked in
- Employee pricing-approval capability stays owner/admin-only (`canApproveEstimatePricing`).
- Multi-org accounts default to their **oldest** active membership when no explicit preference is set.
- Direct-work-order creation stays owner/admin-only (`canCreateDirectWorkOrder`) — verified live during Demo population (employee and subcontractor both correctly denied).
- Demo dataset payments use only the manual/offline "check" method, clearly labeled fictional — no payment provider is connected for Demo.

## Known Issues and Limitations
1. `/auth/confirm/route.ts` — necessity is determined by Supabase's dashboard-configured email templates, not app-code references.
2. E2E coverage gap (pre-existing): `tests/e2e/portal-auth-bot.spec.ts` uses `generateLink()`, a different URL shape than the real configured email template.
3. Brandon Fleenor has a real, unconfirmed auth user and one currently-pending PPM invite (expires 2026-08-15) — not yet added to Demo, per instruction.
4. `docs/HANDOFF-current.md` and `docs/IMPLEMENTATION-STATUS.md` are stale/superseded.
5. `employee-onboarding-admin-invite-bot.spec.ts` and `data-consistency-bot.spec.ts`'s invoice-total test are both known-flaky, pre-existing, unrelated to any work in this session.
6. HEIC/HEIF photo uploads are not supported (sharp's prebuilt binary limitation).
7. **Production application transactional email (Resend) is not configured** (`RESEND_API_KEY` unset). Needs a deliberate decision on whether/when to configure it. Not touched during Demo population.
8. `canManageInspectionTemplates` capability exists but is intentionally unused — no template-management RPC/UI ships yet.
9. Numbering sequences (`service_request_number_seq`, `estimate_number_seq`, `invoice_number_seq`) are **global across all organizations**, not per-org — confirmed in practice: Demo's requests came in as `SR-000009`–`SR-000011`, continuing PPM's counter. Pre-existing platform behavior. **Recommended before Platform v1.0**: per-organization numbering.
10. No public-facing intake path exists for Demo (public API routes are correctly PPM-specific) — Demo service requests were created by calling the real `createServiceRequest()` function directly with Demo's `orgId`, a documented exception since no public form exists to submit through.
11. The temporary Demo population "driver" auth user could not be deleted (FK-referenced as audit-trail actor on 32 permanent records) — retained inert (zero memberships, banned, synthetic email). See the Demo dataset manifest's Stage 5 section.
12. The working invoice is not auto-seeded from an accepted quote — staff must manually confirm the quoted scope onto it via the standard line-item editor. Intentional architecture (actuals/extras ledger), not a defect.

## Environment State
- **premier-crm-prod** (`apnbpcauqrjvkoleisde`): live, serving commit `44cd6d5`. 2 organizations (PPM `a0000000-...` blank, Demo `a0c9b59d-...` fully populated with a permanent dataset). PPM confirmed blank across every entity type checked throughout population.
- **premier-crm-e2e** (`slbnizoskumwhleeiccv`): has all migrations through this phase applied and verified.
- Production CRM: `app.ppmnky.com`, serving commit `44cd6d5`.
- Production marketing site: `www.ppmnky.com` / `ppmnky.com`, serving commit `8807fa9` (unchanged this phase).

## Test Status (final, this phase — post-deployment)
Re-run against merged `main` (`8da54d7`) after production deployment: `pnpm typecheck` clean across all packages; `pnpm test` **159/159**. Production build confirmed via the successful Vercel deployment of this exact commit (`dpl_CRw8Pbyb7A8pGjEgsgYKZUeE6nAz`, READY). `apps/web/lib/branding.test.ts` covers brand constants, root/per-route title config, PWA manifest, staff sign-in heading, portal tenant-branding, and org-switcher separation; two cases in `apps/web/lib/email.test.ts` prove the internal staff-notification template says Forge and every customer-facing template still says Premier Property Maintenance, never Forge/Foundry. Repository-wide naming search re-run post-deploy — all remaining "Premier CRM" matches classified as historical narration, stable technical identifiers (Supabase/Vercel refs, migration files), or deferred non-customer-facing test-suite comments; no unresolved product-facing text found. Migration `20260803060000_forge_demo_org_display_name.sql` applied to both `premier-crm-e2e` (dry-run, 0 rows — org never bootstrapped there) and `premier-crm-prod` (real rename, confirmed by direct query: Demo → "Forge Demonstration", PPM unchanged, all Demo data counts intact).

## Forge V1.0.1 Authorization Hardening (2026-08-03)

Since the Phase F2 checkpoint above, the following happened (not yet reflected in the sections above — see `docs/security/service-requests-authorization-audit.md` §14 for full detail):

- **Forge V1 readiness audit completed** (PR #91, merged `2dbe8a7`), including a Batch A production-verification addendum (PR #93, `24bf3d4`).
- **`forge-v1.0.0` tag created** at `9181d56` (PR #94/#95, `fb56827`).
- **Batch A jobs/quotes authorization hardening** was implemented and deployed to production prior to the tag (grants revoked, broad `FOR ALL` policies replaced with SELECT-only, matching the pattern below).
- **`service_requests`/`estimates`/`site_visits` authorization audit completed** (PR #96, merged `5f6b7c1`) — read-only, found the same broad-write vulnerability class as Batch A on `service_requests` and `estimates` (confirmed live), and a stale-but-inert RLS policy on `site_visits` (grant-layer already safe, predating the audit).
- **Forge V1.0.1 implemented, merged, deployed, and verified in production (2026-08-03).** PR #97 squash-merged to `main` at commit `2448026`. Vercel production deployment `dpl_5QEtPdyZ5YmiXHrXx9YJoco2e1Zj` READY, aliased to `app.ppmnky.com`, serving `2448026`. Migration `20260803080000_harden_service_requests_estimates_site_visits.sql` applied to `premier-crm-prod` via `npx supabase db push --linked` (`2026-08-03T18:22:09Z`–`18:22:28Z`) — sole pending migration, dry-run confirmed, applied cleanly, migration history back in sync. Production grants/policies verified directly: `authenticated` now SELECT-only on `service_requests`/`estimates`/`site_visits`; 12 direct-write denial probes + 2 read checks executed against production using real accounts (Demo owner/employee, PPM admin as cross-org actor) inside rolled-back `SET LOCAL role` transactions — all denied (`42501`), zero side effects, record counts/row states unchanged. `subcontractor`/`viewer` coverage remains E2E-only (no such accounts exist in production). PPM confirmed still blank; Demo confirmed healthy and unchanged (4 service_requests/5 estimates/2 site_visits/2 customers/2 jobs). Post-merge `pnpm test` 187/187, typecheck clean, build clean, new auth E2E 30/30, `customer-intake-bot` 7/7, affected regression suites all pass except the same pre-existing `employee-estimate-workflow-bot.spec.ts` test 12 locator-timing flake (confirmed via isolated re-run, unrelated to this patch). Full detail: `docs/security/service-requests-authorization-audit.md` §14–§15, `docs/releases/forge-v1.0.1-release.md`. **Verdict: READY WITH NON-BLOCKING FOLLOW-UPS.**
- **`forge-v1.0.1` tag created** (annotated) at `d5e9824` — the documentation-complete checkpoint commit, one commit past the `2448026` application-code baseline (PR #98, docs-only). GitHub Release [`forge-v1.0.1`](https://github.com/Bozo012/Premier-CRM/releases/tag/forge-v1.0.1) published (not draft, not prerelease). `forge-v1.0.0` unchanged at `9181d56`.

## Forge V1.0.2 Customers/Properties Authorization Hardening (2026-08-04) — deployed and verified in production

- **`customers`/`properties`/`customer_properties`/`customer_accounts` authorization audit completed** (PR #100, squash-merged to `main` at `4e9ef666`) — read-only, found the same broad-write vulnerability class as Batch A/V1.0.1 on `customers`/`properties` (CP-1/CP-5), plus a distinct, more serious cross-org relationship-integrity gap on `customer_properties` (CP-2) and `customer_accounts` (CP-3) — the latter a genuine cross-tenant data-exposure path via the customer-portal SELECT policies, not just a tampering risk. Full detail: `docs/security/customers-properties-authorization-audit.md`.
- **Batch CP-A (customers/properties direct-write revoke)** and **Batch CP-B (customer_properties/customer_accounts cross-org relationship fix)** implemented on branch `fix/customers-properties-authorization-hardening`: `20260804000000_harden_customers_and_properties.sql`, `20260804000001_harden_customer_properties_and_accounts.sql`, and a same-session bug-fix migration `20260804000002_fix_customer_properties_accounts_policy_recursion.sql` (CP-B's first version caused an RLS policy-recursion cycle — `42P17` — caught by the new test suite in E2E before touching any shared/production environment; fixed with two `SECURITY DEFINER` helper functions matching the existing `user_is_in_org()` pattern). All three migrations applied and verified on `premier-crm-e2e` first via direct SQL execution (not `db push`, due to E2E's pre-existing, unrelated migration-bookkeeping drift — 27 migrations recorded under different version timestamps than their local filenames, live schema unaffected; **still intentionally unreconciled**, does not touch production).
- **Forge V1.0.2 implemented, merged, deployed, and verified in production (2026-08-04).** PR #101 squash-merged to `main` at commit `fba6b7e`. Vercel production deployment `dpl_5dRrqLCUGvrYnZkL7nXM7UTkDvCq` READY, aliased to `app.ppmnky.com`, serving `fba6b7e`. All three migrations applied to `premier-crm-prod` via `npx supabase db push --linked` (`2026-08-04T01:13:56Z`–`01:14:40Z`) in one controlled run — production had zero migration drift going in, dry-run confirmed exactly these three pending, migration-history row count went 73→76 (+3 exactly). Production grants/policies verified directly: `authenticated` now SELECT-only on all four tables; helper functions `customer_org_id()`/`property_org_id()` present and verified safe (search_path-constrained, fail-closed on NULL/nonexistent IDs); recursion fix confirmed live (zero `42P17` across all four tables under simulated `authenticated` RLS evaluation). 17 direct-write/cross-org denial probes + 4 read checks executed against production using real accounts (Demo owner/employee, PPM admin as cross-org actor, the real portal `customer_accounts` auth user) inside rolled-back `SET LOCAL role` transactions — all denied (`42501`), all reads correctly scoped, zero side effects, record counts/row states unchanged. `subcontractor`/`viewer` coverage remains E2E-only (no such accounts exist in production). PPM confirmed still blank; Demo confirmed healthy and unchanged (2 customers/3 properties/3 customer_properties/1 customer_accounts). Post-merge `pnpm test` 187/187, typecheck clean, build clean, new auth E2E 30/30, adjacent regression suites 47/47 (7 pre-existing TODO-skips), zero unexplained failures. Full detail: `docs/security/customers-properties-authorization-audit.md` §16–§17, `docs/releases/forge-v1.0.2-release.md`. **Verdict: PASSED — READY WITH NON-BLOCKING FOLLOW-UPS.**
- **`forge-v1.0.2` tag created** (annotated) at `734e5f1` — the documentation-complete checkpoint commit (PR #102, docs-only), one commit past the `fba6b7e` application-code baseline, matching the `forge-v1.0.1` precedent exactly. GitHub Release [`forge-v1.0.2`](https://github.com/Bozo012/Premier-CRM/releases/tag/forge-v1.0.2) published (not draft, not prerelease). `forge-v1.0.0` unchanged at `9181d56`; `forge-v1.0.1` unchanged at `d5e9824`.
- **Base44 compatibility spike completed and approved as an architectural reference (2026-08-04).** Branch `spike/base44-today-compat` (HEAD `a05700e`), unmerged, isolated from `main`. Proved a visually distinct presentation layer can sit on Forge's existing auth/org-isolation/capability/query/RPC/lifecycle/mutation boundary via a 3-layer pattern (existing domain code / pure adapter view-model / replaceable presentation). Verdict: **COMPATIBLE WITH CONSTRAINTS**. One real defect found — spike's `_lib/view-model.ts` relocated a genuine workflow-relevance rule into Layer 2 instead of `packages/db`; **now corrected in the real Today implementation** (not on this branch, which stays reference-only). Full detail: `docs/ux/base44-compatibility-spike-report.md`.

## Forge V1.1 UX Modernization — Batch UX-A and Today (PR2) both merged and verified (2026-08-04)

- **Kevin approved expanding the spike into a full Forge V1.1 UX modernization program**, prioritized: (1) Today, (2) Estimates/pricing review, (3) Site inspections/field work — then the same design system extended to every remaining staff and customer-portal route.
- **Complete plan**: `docs/ux/forge-v1.1-ux-modernization-plan.md` — full route inventory, UX audit, shared design system, batches UX-A–UX-E, Today/Estimates/Site-Inspection requirements, Base44 integration contract (`base44-handoff.md` §18).
- **Batch UX-A (shared foundation) merged.** PR #104 squash-merged to `main` at `05fbead`. Shipped: 7 shared UI primitives (`status-pill`, `page-header`, `empty-state`, `error-state`, `access-denied-state`, `loading-skeleton`, `form-field`) and the app's first-ever desktop/tablet navigation shell (`app-desktop-nav`, `app-shell`), wired into `(app)/layout.tsx`.
- **Batch UX-B, PR2 (Today) merged and verified.** PR #105 squash-merged to `main` at commit `d9c9ff1`. Corrected the spike's relocated workflow-rule defect before merge (`getTodayQuoteActivity()` lives in `packages/db/queries/today-actions.ts`, alongside `getTodayActionItems()` — confirmed via repo-wide grep that no duplicate of the rule exists anywhere else). Applied Kevin's Today-specific decisions: actionable-only operational counts (no accounting totals), capability-filtered quick actions, removed navigation now duplicated by the UX-A desktop nav, merged jobs+site-visits into one schedule. Fixed the phone-overflow bug found during UX-A's own verification — explicitly re-verified at 390×844 post-merge via a real Playwright viewport assertion, not assumed from a screenshot. A pre-merge review pass caught and fixed a real gap (the Today components were missing their documented `BASE44-REPLACEABLE` markers) before merging. **Merged-main validation**: `pnpm test` 205/205, typecheck/build clean, 27/27 E2E across `today-redesign-bot`/`today-action-queue-bot`/`operator-workflow-bot`/`multi-org-switching-bot` on a clean re-run (one transient first-pass contention flake in `operator-workflow-bot`, confirmed non-reproducing on isolated and full-batch re-runs). Full detail: `docs/ux/forge-v1.1-today-redesign.md`.
- **The merged Today implementation (`d9c9ff1`) is now the authoritative Forge V1.1 presentation reference — not the spike branch.** `spike/base44-today-compat` (HEAD `a05700e`) remains separate, unmerged, historical/reference-only; confirmed not present in `main`'s history.
- **Two pre-existing, unrelated E2E issues found, documented, and deliberately not folded into either PR**: `/customers` list search flakiness (`staff-permissions-bot`, `employee-estimate-workflow-bot`) and a stale `"Sign in to Premier"` login-heading selector from an earlier Forge rebrand (`mobile-simplicity-bot`) — both confirmed present on unmodified merged `main` before any Forge V1.1 work began. Tracked as separate follow-ups, not fixed here.
- **PR3 (Estimates) and PR4 (Site Inspection) not started.** **No Base44 GitHub connection has been made.** No actual Base44-generated output exists yet. Photo backend (site-visits) remains functional and untouched; dictation remains 100% unbuilt.
- **Base44 readiness checkpoint recorded** ahead of the (separate, not-yet-run) Base44 connection/readiness task: authoritative main commit `d9c9ff1`; Today's Layer 3 (`apps/web/app/(app)/today/_components/*.tsx`, all marked `BASE44-REPLACEABLE`) is the intended first real Base44 design target; Layer 1 (`packages/db/queries/today-actions.ts`) and Layer 2 (`apps/web/app/(app)/today/_lib/view-model.ts`) stay Forge-controlled and are never to be owned by Base44 output; Base44 must not connect directly to Supabase, must not receive production secrets, and must not own workflow/authorization rules.

## Base44 Builder Readiness Documentation — merged (2026-08-04)

- **Base44 Builder readiness plan and presentation contracts merged.** PR #107 merged (`--merge`, fast-forward) to `main` at commit `00dae25`. Files: `docs/ux/base44-builder-readiness-plan.md`, `docs/ux/base44-presentation-contracts.md`. Docs-only — no Forge application code touched.
- **Readiness verdict: READY AFTER LISTED ACTIONS.** Recommended isolation strategy: dedicated repository `Bozo012/Forge-Base44-UX`, direct connection to `Premier-CRM` explicitly rejected. Full behavior verification, repository-safety comparison (options A–E), presentation-contract package (`TodayViewModel` grounded in the real merged Today implementation; `EstimateViewModel`/`SiteInspectionViewModel` forward-looking), component-transfer workflow + security screening checklist, one-month execution plan, downgrade/export plan, click-by-click setup steps, and first Base44 prompt are all recorded in the two merged docs.
- **Account-side Base44 setup remains pending** — not performed this session. Base44 is **not connected** to Premier-CRM (nor to any repository). `Bozo012/Forge-Base44-UX` **has not yet been created**. Estimates (PR3) and Site Inspection (PR4) **remain unstarted**.

## Base44 Today Sync Verification & Portability Audit (2026-08-04)

- **Kevin completed the account-side Base44 setup.** Base44 app "Forge Today / Forge UX Workspace" created; `Bozo012/Forge-Base44-UX` exists on GitHub (private, default branch `main`); Base44 generated a complete Today component set and froze it as the approved visual reference.
- **GitHub sync state independently verified, not trusted from Base44's chat claim** — classified **A: files already committed to remote `main`**. `Forge-Base44-UX` cloned locally to `C:\dev\Forge-Base44-UX` (separate from `Premier-CRM`); local `main` matches remote HEAD `adee72e` exactly, zero drift. All 14 listed Today components + contract + theme CSS + docs present remotely; the 3 files Kevin flagged as must-not-enter-Forge (`TodayRoute.tsx`, `todayScenarios.ts`, `forgeToday.ts`) are also present remotely (correctly, as workspace-only) but were not, and must not be, ported.
- **Repo-evidence permission check**: no Base44 webhook or bot collaborator found on `Premier-CRM` or `premier-property-maintenance`; `Forge-Base44-UX` has exactly one webhook (`app.base44.com`, push events only) and one human collaborator (`Bozo012`). One manual step remains for Kevin: visually confirm the Base44 GitHub App's installation-scope screen shows only `Forge-Base44-UX` (this session's API credentials could not enumerate that directly — `user/installations` returned 403).
- **Full portability audit completed**: zero Base44 SDK/Supabase/secret contamination found in the Today component tree; one real defect found — `useForgeAppearance.ts` implements a second, independent `localStorage`-based appearance-persistence system, and `MobileNavigation.tsx` hardcodes its own nav-item list instead of taking it as a prop. `DesktopNavigation.tsx`/`MobileNavigation.tsx`/`BrowseLinks.tsx` are recommended **not** to be ported as components (would fork/duplicate Forge's existing shared UX-A nav, or reintroduce a browse-grid duplication Kevin already removed) — theming-only follow-ups to the existing shared shell, at most.
- **One open product decision surfaced, not decided**: Forge currently has **no existing light/dark/system appearance system** at all (`darkMode: ['class']` is configured in Tailwind but unused — no ThemeProvider, no toggle anywhere in the app). Whether Forge V1.1 introduces a real appearance toggle this cycle, or the burnt-orange palette simply becomes the new fixed default look, is Kevin's call before `ThemeControl.tsx` can be integrated.
- **Full audit, Base44-to-Forge component mapping, theme-integration plan, and proposed (unimplemented) integration plan**: PR #109, `docs/ux/base44-today-sync-and-portability-audit.md` — **open, not yet merged**, awaiting Kevin's review alongside the two open product decisions above. **Verdict: READY AFTER LISTED FIXES.**
- **No code was integrated into Premier-CRM. No push was made to `Forge-Base44-UX`. Estimates and Site Inspection remain unstarted.**

## Base44 Today Visual Integration — merged and verified (2026-08-04)

- **PR #109 merged.** `docs/ux/base44-today-sync-and-portability-audit.md` + `docs/SESSION_STATE.md` merged to `main` at commit `cc795b5` (`gh pr merge --merge`).
- **Kevin's two decisions resolved**: (1) Forge V1.1 gets a real, Forge-owned Light/Dark/System appearance setting, defaulting to System, client-side-only persistence, Base44's `useForgeAppearance.ts` not ported. (2) Base44 GitHub App scope manually confirmed by Kevin — `Forge-Base44-UX` only, no access to `Premier-CRM`/`premier-property-maintenance` — no further confirmation requested.
- **Integration branch**: `feature/forge-v1.1-today-base44-visual-integration`, from `main` @ `cc795b5`. Base44 source verified unchanged at `adee72ef881be3023ef78332c28540b2326b3a89` immediately before implementation (no new commits since the audit).
- **Implemented**: Today's Layer 3 (`today-header`, `action-queue`, `today-schedule`, `snapshot-grid`, `quick-actions`) adopted the approved Base44 visual treatment; Forge's shared CSS-variable tokens (`app/globals.css`, `tailwind.config.ts`) extended with the burnt-copper/near-black palette plus new `warning`/`success`/`nav-*`/status-surface tokens; `StatusPill` re-mapped onto the new tokens (tone API unchanged, every existing consumer app-wide now dark-mode-correct for free); new Forge-owned `ThemeProvider`/`ThemeControl` (`apps/web/components/theme/`), anti-flash inline script in `app/layout.tsx`; `AppDesktopNav`/`AppBottomNav` re-themed (icons + nav tokens), route/active-state/capability logic in both left **completely unchanged** — Base44's `DesktopNavigation.tsx`/`MobileNavigation.tsx` were **not** ported as components (would have forked the shared nav; `MobileNavigation.tsx` also hardcoded its own item list). Layer 1 (`packages/db/queries/today-actions.ts`) and Layer 2 (`apps/web/app/(app)/today/_lib/view-model.ts`) **untouched**.
- **Validation**: `pnpm test` 205/205 unaffected; `pnpm --filter web typecheck` clean; `pnpm --filter web build` clean (dev server stopped first). E2E run against `premier-crm-e2e` (dev server started with explicit env vars exported from `.env.test` — **`apps/web/.env.local` was found still pointing at `premier-crm-prod`'s project ref, left untouched, not relied on**; verified via `/api/e2e-health` that the running server was actually on `premier-crm-e2e` before any test ran): `today-redesign-bot` 20/20 (one selector updated — a value element's tag/class changed as part of the visual adaptation, structural CSS selector replaced with a scoped text assertion, per explicit instruction), `today-action-queue-bot` 7/7 unmodified, `multi-org-switching-bot` 7/7, `operator-workflow-bot` 1/1, new `today-appearance-bot.spec.ts` 6/6 (System default, Light/Dark override, live system-preference response, reload persistence, keyboard accessibility).
- **Documentation updated**: `docs/ux/base44-today-sync-and-portability-audit.md` §13 (full integration record), `docs/ux/forge-v1.1-today-redesign.md` (new "Base44 Today visual integration" section), `docs/ux/forge-v1.1-ux-modernization-plan.md` (status line), this file.
- **PR #110 merged.** https://github.com/Bozo012/Premier-CRM/pull/110 — merged (`--merge`, fast-forward) to `main` at commit `daa917b`. Vercel's normal GitHub-integration behavior automatically deployed `daa917b` to **production** (`dpl_FEQGMN1vaPgpwnVUUD1RxEnUqbwg`, READY) — no manual deploy performed.
- **Post-merge validation on `main` @ `daa917b`**: `pnpm test` 205/205, typecheck/build clean. E2E against `premier-crm-e2e` (dev server started with explicit env vars, `/api/e2e-health` confirmed `slbnizoskumwhleeiccv` before testing): `today-redesign-bot` 20/20, `today-action-queue-bot` 7/7, `today-appearance-bot` 6/6, `multi-org-switching-bot` 7/7, `operator-workflow-bot` 1/1 (one transient worker-contention timeout on the first 5-worker parallel run, in the unrelated `/invoices` route — confirmed non-reproducing via an isolated single-worker re-run).
- **The Base44 Today visual integration is now the authoritative, live Forge V1.1 Today presentation** — Light/Dark/System appearance available app-wide, Forge-owned shared nav re-themed, Layer 1/2 untouched throughout.
- **Documentation checkpoint recorded**: `docs/ux/base44-today-sync-and-portability-audit.md`, `docs/ux/forge-v1.1-today-redesign.md`, `docs/ux/forge-v1.1-ux-modernization-plan.md`, this file — all updated to reflect the merge, via one focused docs-only PR.
- **Estimates (PR3) is next**, then Site Inspection (PR4) — both **not started**, pending Kevin's separate authorization to begin. Photo/dictation functional work remains entirely pending. **No `forge-v1.1` tag exists.**

## Next Exact Step
Both Batch UX-A and Today (PR2), the Base44 readiness docs (PR #107/#108), the Today sync/portability audit (PR #109), and the Today Base44 visual integration (PR #110) are all merged and verified on `main` @ `daa917b`, live in production. Next, pending Kevin's explicit authorization: begin the **Estimates (PR3)** redesign, following the same audit → port → bind → test → PR workflow this Today integration just proved out. Site Inspection (PR4) follows after Estimates. Also pending Kevin's separate authorization: deciding whether to audit or defer `customer_location_prefs`; addressing F2/F4/F6/F7 from the original Forge V1 readiness audit; reconciling E2E's pre-existing migration-bookkeeping drift; fixing the two documented pre-existing/unrelated E2E issues, only if ever prioritized; fixing `apps/web/.env.local`'s stray production Supabase pointer (found, not touched, this session — a standing hazard for anyone running `pnpm dev` without the explicit-env-var pattern). Brandon's further onboarding remains separately not authorized. No Forge V1.1 tag until the full program (or an explicitly agreed subset) ships and is verified.

## Checkpoint Routine (unchanged)
Update this file after each meaningful milestone and before/after any production action. Commit in small logical checkpoints. Never commit secrets. Never leave important decisions only in chat history.

## 2026-08-05 — Customer portal marketing handoff checkpoint

Implemented the real cross-repo customer portal boundary: the marketing site
remains the public `/customer-portal` doorway, while Forge owns form POST
handoff endpoints, Supabase Auth/session cookies, customer-account
match-or-create linking, password recovery, and the authenticated
`/portal/dashboard`. Forge `/portal` and `/portal/login` are redirect-only to
avoid a second customer sign-in landing. Forge also exposes
`/portal/handoff/sign-up` as a first-class alias for the existing
request-access signup flow, so the marketing site can use customer-facing
"Sign Up" labels without changing account-linking behavior. No database
migrations, merges, or deployments were performed in this checkpoint.

Also re-verified the existing public service-request intake: the marketing
site `/request-service` form posts to Forge CRM `POST /api/v1/service-requests`
via `VITE_CRM_API_URL`; Forge validates the website payload, applies CORS,
honeypot, and rate-limit protections, creates or deduplicates the CRM
customer/property records, links them, and inserts the `service_requests` row.
