# Session State

## Last Updated
2026-08-02 (Checkpoint B complete; final merge-readiness audit performed — 4 real cross-org authorization defects found and fixed; PR #80 ready for review, awaiting Kevin's approval to merge)

## One coherent story so far

1. **Production stabilization (Milestone A) — ACHIEVED.** PR #78 (portal auth, working-invoice DB lock, client-error instrumentation, timeline, scheduling toast fix) and PR #79 (missing `/auth/confirm` route, found live) both merged and deployed. All six Milestone A live-verification items confirmed on production: deployment serves the merged commit, `/api/e2e-health` 404s, portal signup works end-to-end, password reset works end-to-end (a third real defect — Auth email templates linking directly to `supabase.co`, exposing them to link-prescanning — found and fixed via Supabase dashboard config, no code change), client-error logging reaches Vercel runtime logs, timeline renders real entries on a real production job.
2. **Production cleanup (Phase 1/2) — EXECUTED AND VERIFIED.** Both Approval A (general smoke-test/dev-artifact cleanup) and Approval B (the $2.00 paid smoke-test invoice + its full dependent change-order/audit chain, granted mid-execution) completed. Full record: `docs/production/cleanup/2026-08-01-production-cleanup.md`.
3. **Full Jobber-import purge — EXECUTED AND VERIFIED, same day, broader scope change.** Kevin explicitly reclassified the previously-preserved 38 `jobber_import` customers and 43 linked properties as legacy import/test data, not trusted production history, and approved full deletion. Addendum in the same cleanup doc. **Production is now a true blank slate** for customer/property/workflow data — only org config, real staff (`organizations`=1, `org_members`=4), schema/RLS/permissions/infrastructure (including 4 supplier geofences) remain.
4. **Storage/upload architecture checkpoints (A and A.1) — verified via dedicated spikes against `premier-crm-e2e`, all spike scaffolding removed afterward, then implemented for real.** Quarantine-then-finalize architecture: client uploads the original to a private pending path; a trusted server action (`apps/web/lib/site-visit-attachments.ts`, the only file importing `sharp`) downloads, validates actual content, processes with `sharp` (auto-orient + full EXIF/GPS strip), and only then writes the sanitized permanent object and creates the `vault_items` row. Real-phone orientation test passed. HEIC/HEIF confirmed unsupported by sharp's prebuilt binary — MIME allow-list narrowed to JPEG/PNG.
5. **Site-visit workflow — backend, Storage, staff UI, customer portal, and permanent test coverage are all COMPLETE (Checkpoint B).** 18 migrations, a full RPC-only mutation surface, a complete staff UI (request triage panel, site-visit detail page, estimate review with line items and pricing approval), a customer portal presentation using only `get_my_site_visit_summary()`, 19 E2E tests + 6 upload-integration tests + capability parity + DST/timezone tests, full existing-suite regression check. **Two real regressions found and fixed**: (a) the quote-eligibility trigger initially gated every estimate-linked quote, breaking the pre-existing manual-estimate flow — narrowed to only gate triage-originated estimates; (b) `correct_request_triage()`'s `record IS NOT NULL` check silently never fired due to a genuine PL/pgSQL row-wise-NULL-test pitfall — fixed by checking the primary-key column instead (`20260802020700_fix_correct_triage_record_null_check.sql`), found by the new triage-correction test. Full report: `docs/implementation/request-site-visit-estimate-workflow.md`.
6. **Marketing-site (second repo) public-intake fix**: pushed to `fix/state-code-validation` on `premier-property-maintenance` (formerly `PPMSITE`), commit `e87976a`, draft PR [#5](https://github.com/Bozo012/premier-property-maintenance/pull/5). **Not merged, not deployed.**
7. **Final merge-readiness audit performed on PR #80** — an independent senior-review pass (fresh code/security review + full migration review, each done blind to the implementation report's claims) found the SQL/RPC layer clean but found **4 real blocking defects in the TypeScript layer**, all cross-org authorization gaps: (a) `getSiteVisitById()` had no org filter and was called with the service-role client — any staff user of any org could read any other org's site-visit detail by URL; (b) `saveSiteVisitInspectionAction()` never verified the target visit's org before writing inspection findings via the service-role RPC; (c) the `remote_estimate` triage path had no working UI to approve pricing, so its quote-creation button always failed; (d) the legacy quote-creation action checked the wrong capability (`canCreateEstimates` instead of `canCreateQuote`), letting subcontractors bypass the quote-creation restriction. **All 4 are fixed and verified** — see `docs/implementation/request-site-visit-estimate-workflow.md` §13. A related lower-severity org-isolation gap in the upload-quarantine path was fixed at the same time.
8. **Stopping point reached**: Checkpoint B plus the merge-readiness audit are both complete. PR #80 has been moved from Draft to **Ready for Review**. **Production deployment, PR merge, Demo org creation, Brandon onboarding, and Platform v1.0 tagging remain explicitly not authorized** — awaiting Kevin's review and approval.

## Current Branch
`feature/request-site-visit-estimate-workflow`, created fresh from `origin/main`. Draft PR #80 open. Local HEAD includes the full Checkpoint B backend + UI + portal + test-coverage commits.

**Important housekeeping note**: `docs/SESSION_STATE.md`, `docs/CLAUDE_CONTEXT.md`, `docs/RESUME_PROMPT.md`, and `docs/production/cleanup/2026-08-01-production-cleanup.md` previously existed only on a separate local branch, `fix/auth-confirm-route`, which was **never pushed or merged to `origin/main`** even though its one real PR (#79, commit `6314024`/merged as `81dd9da`) already landed. This was reconciled onto this branch earlier in Checkpoint B. `fix/auth-confirm-route` itself is now safe to delete once its content is confirmed fully captured here — not done yet, flagged for a deliberate decision rather than silently deleted.

## Current Goal
Checkpoint B (request → site visit → estimate → quote workflow) is complete per the approved plan at `C:\Users\somme\.claude\plans\mighty-watching-raven.md`. Awaiting review and approval before any production action.

## Business policy decisions locked in during Checkpoint B
- **Employee pricing-approval capability stays owner/admin-only** (`canApproveEstimatePricing`). This is the deliberate initial business policy, not an unresolved blocker — employees get `canEditEstimate`/`canCreateQuote`/`canSendQuote` but never pricing approval. Subcontractors get only `canEditEstimate` (findings/draft scope), never quote creation or sending. Revisit only on an explicit future decision.

## Known Issues and Limitations
1. `/auth/confirm/route.ts` — its necessity is determined by Supabase's *dashboard-configured* email templates, not by grepping app code for references (it was deleted once as apparently-dead code, then needed again). Do not repeat that mistake.
2. E2E coverage gap (pre-existing, understood): `tests/e2e/portal-auth-bot.spec.ts` uses `generateLink()`, which returns a different URL shape than the real configured email template produces — this is why the full suite passing didn't originally catch the `/auth/confirm` 404.
3. Brandon Fleenor's staff invite (`b373d9a3-2cc0-4eb4-9ec3-3e8ef1a5c7b6`, `brandonjfleenor28@gmail.com`) may still need a fresh resend via `/team` — not re-verified since Milestone A.
4. Email-link prescanning can silently consume single-use auth tokens (mitigated by routing all auth email links through `/auth/confirm` on our own domain instead of directly to `supabase.co`).
5. `docs/HANDOFF-current.md` and `docs/IMPLEMENTATION-STATUS.md` are stale/superseded — `docs/BASELINE_V1.md`, `docs/ARCHITECTURE_AND_DEVELOPMENT_GUIDE.md`, and this file are current ground truth.
6. **`employee-onboarding-admin-invite-bot.spec.ts`** — see `docs/implementation/request-site-visit-estimate-workflow.md` §12 for its final re-run result and regression-comparison against `origin/main`.
7. HEIC/HEIF photo uploads are not supported (sharp's prebuilt binary limitation) — JPEG/PNG only for v1.
8. Kevin's staff account (`kevinsommers@ppmnky.com`) may still be on the Claude-set temporary password from the Milestone A investigation — no urgency, "Forgot password" whenever convenient.
9. Migration version numbers recorded in `premier-crm-e2e` don't match this branch's local migration filenames (an artifact of the raw `apply_migration` MCP tool vs. `supabase db push`) — cosmetic to the e2e sandbox only, documented in the implementation doc §9.

## Environment State
- **premier-crm-prod** (`apnbpcauqrjvkoleisde`): blank slate for customer/property/workflow data (see story item 3 above). No migration from this workflow has been applied here. No write of any kind performed against it during Checkpoint B.
- **premier-crm-e2e** (`slbnizoskumwhleeiccv`): has all 18 Checkpoint B migrations applied and verified; confirmed clean of all spike/test residue after each checkpoint.
- Local dev server: running, pointed at premier-crm-e2e (`/api/e2e-health` confirms project ref `slbnizoskumwhleeiccv`).

## Test Status (final, post-audit)
See `docs/implementation/request-site-visit-estimate-workflow.md` §12-13 for the complete, authoritative validation results. Summary: `pnpm typecheck` / `pnpm --filter web build` clean; `pnpm test` 104/104; workflow bot 20/20 (10 golden-path + 1 new IDOR regression test + 8 lifecycle-guards/corrections/DWO + 1 capability-parity); full E2E suite 122 passed / 3 failed + 14 did-not-run (all confined to the pre-existing `employee-onboarding-admin-invite-bot`, confirmed via a clean `origin/main` worktree comparison to fail identically there) / 24 skipped; invoice-flake test independently re-run 4× (1 fail / 3 pass, ~25% pre-existing flake rate, unrelated route); marketing-site `tsc`/`vite build` clean.

## Next Exact Step
Checkpoint B and its merge-readiness audit are both complete and reported. PR #80 is Ready for Review. Awaiting Kevin's explicit review/approval before: merging PR #80, applying migrations to `premier-crm-prod`, deploying `apps/web` or the marketing site, creating the Demonstration organization, onboarding Brandon, or tagging Platform v1.0.

## Checkpoint Routine (unchanged)
Update this file after each meaningful milestone and before/after any production action. Commit in small logical checkpoints. Never commit secrets. Never leave important decisions only in chat history.
